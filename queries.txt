USE tc1_Devon_Brewster;
USE students;
DROP database tc1
DROP TABLE tekcampers;
CREATE TABLE tekcampers2(id INT AUTO_INCREMENT NOT NULL PRIMARY KEY, first_name VARCHAR(20), last_name VARCHAR(20), gender CHAR, education VARCHAR(30));

SHOW TABLES;
SELECT * FROM tekcamp01;
INSERT INTO tekcampers
SELECT * FROM tekcamp01;

SELECT * FROM tekcampers;
SHOW TABLES;
SELECT * FROM tekcampers2;

SELECT COUNT(last_name) > 7 FROM tekcampers2;
SELECT * FROM tekcampers2 ORDER BY first_name;
SELECT * FROM tekcampers2 ORDER BY last_name;
SELECT * FROM tekcampers2 WHERE gender = 'M';
SELECT * FROM tekcampers2 WHERE gender = 'F';
SELECT * FROM tekcampers2 WHERE education= 'Master';
SELECT * FROM tekcampers2 WHERE education= 'Bachelor';
SELECT * FROM tekcampers2 WHERE education= 'Associate';
SELECT * FROM tekcampers2 WHERE education != 'Associate' && education !=  'Bachelor' && education= 'Master';


CREATE TABLE ta_Emilios(id INT AUTO_INCREMENT NOT NULL PRIMARY KEY, first_name VARCHAR(20), last_name VARCHAR(20), gender CHAR, education VARCHAR(30));
 INSERT INTO ta_Emilios(first_name, last_name, gender, education)
 VALUES ('Devon', 'Brewster', 'M', 'some college')
,('Abigail', 'Swigert', 'F', 'Bachelor')
,('Gabe', 'Chavez', 'M', 'Bachelor')
,('Phoenix', 'Swigert', 'F', 'Associate')
,('Jose', 'Moreno', 'M', 'bachelors')
,('Adam', 'Rice', 'M', 'some college')
,('Marcelo', 'Barbosa', 'M', 'some college')
,('Julian', 'Ladd', 'M', 'some college')
,('Phoenix', 'Swigert', 'F', 'Associate')
,('Monica', 'Howard', 'F', 'bachelors')
,('Michael', 'McKinney', 'M', 'some college')
,('Christian', 'Domingo', 'M', 'Associate')

SELECT * FROM ta_Emilios;
CREATE TABLE hobbies(id INT AUTO_INCREMENT NOT NULL PRIMARY KEY, name VARCHAR(20), FOREIGN KEY(ta_Emilios_id) REFERENCES ta_Emilios(id));
 INSERT INTO hobbies(name, last_name, id_ta_Emilios_fk)
 VALUES ('working out', 2)
,('running', 3)
,('video games', 9)
,('watch tv', 23);
SELECT * FROM hobbbies;









scope- determines what should be included in the model, high level categories

model these thins of interest as table

relationships- determines how the tables are related to each other
customers order products
invoices track customer orders

determines cardinality of the relationships
use the appropriate symobls

determin ethe fields eah table needs

determine the keys
-primary
-foreign